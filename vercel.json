// vercel.json (Update this file)
{
  "build": {
    "buildCommand": "vite build"
  },
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
